import React from 'react';
import Login from "../../components/Auth/Login";

const LoginPage = () => {
    return (
        <main id={'LoginPage'}>
            <Login />
        </main>
    );
};

export default LoginPage;